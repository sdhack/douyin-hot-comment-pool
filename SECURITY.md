# Security Policy

## 支持范围（Supported Versions）

| 版本 | 支持状态 |
|------|----------|
| 0.7.x | ✅ 当前活跃，接受安全修复 |
| < 0.7.0 | ❌ 不再维护 |

## 报告漏洞（Reporting a Vulnerability）

请**不要**在公开 issue 中披露漏洞细节。若发现安全性问题，请：

1. 通过邮件/私信方式联系仓库维护者（`sdhack`）。
2. 描述影响范围、复现步骤、建议修复方向。
3. 我们会尽快（目标 < 72 小时）响应，确认后按严重程度排期修复，并在修复落地前对细节保密。

## 已知安全边界（本技能设计使然的说明）

本技能涉及数据采集与凭据，请知悉以下边界并自行采取防护：

- **登录态 / Cookie**：实时采集复用 MediaCrawler 登录态；`--cookies` 可指定 Cookie 文件。**切勿**把 Cookie/Token 文件提交到任何 Git 仓库（`.gitignore` 已默认排除 `*.log` 与运行期产物，请勿额外提交凭证）。
- **只抓公开数据**：仅访问公开可浏览的评论区，不绕过登录墙、不做账号暴力、控制请求频率（`--speed safe` + 随机延时）避免触发风控或对平台造成压力。
- **数据不外带**：打包（zip）只含空 schema 库，真实采集数据与业务信息保留在数据拥有方本地，不随包外发。
- **版权合规**：沉淀的爆款评论仅用于借鉴结构与钩子、重写表达；不建议照搬商用，避免版权风险。

## 报告处理流程

1. 维护者确认漏洞并评估严重性。
2. 修复后发布补丁版本并更新 CHANGELOG。
3. 涉及敏感细节的部分，在合理期限内保密，避免在修复完成前公开利用细节。