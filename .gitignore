# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# SQLite journal (WAL/SHM 会随库生成)
*.db-wal
*.db-shm
*.db-journal

# 运行期产物（勿入库）
*.log
pool/
daily/
candidates.json
comments_aggregated.json

# 系统
.DS_Store
Thumbs.db
# SQLite 库不入版本库（防采集数据外泄；克隆后 db.py --init 重建）
sqlite/*.db

# 真实运行报告含第三方用户昵称（防采集数据外泄）
reports/*运行报告*.html

# 报告产物不再入库（含实测数据/大体积静态资源）
reports/
